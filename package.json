{
  "name": "script-update-record-manually",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && rimraf ./dist && tsc",
    "start:production": "npm run build && node dist/app.js",
    "start": "npm run build && node dist/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "winston": "^3.8.2"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "@types/node": "^17.0.18",
    "@types/supertest": "^2.0.12",
    "@types/winston": "^2.4.4",
    "eslint": "^8.9.0",
    "jest": "^28.1.3",
    "nodemon": "^2.0.15",
    "supertest": "^6.2.4",
    "ts-jest": "^28.0.7",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  }
}
